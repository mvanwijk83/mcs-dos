1. Bug: tried entering "0:" (changing to drive 0) to see what would happen. "Invalid drive specification" is desired behaviour, so that's good - but it's displayed twice.

2. Bug: when using Commodore key to cycle through filenames, the command text is duplicated on a new prompt. Reproduction: typed DEL, then pressed Commodore to cycle. It jumps to a new line, leaving just DEL on the previous one, and DEL with file name on the new one.

3. When using FORMAT, aside from prompting for a volume label, also prompt for a disk ID. If I'm reading the code correctly, it always uses "MC" now.

4. The drive parameter of LABEL should be optional. If omitted, the user will be prompted. RETURN should clear any existing label.
The prompt should follow this format:
Volume in drive n: is LABEL
Disk ID is XX
Volume label  (16 characters)?

5. Change CHKDSK output according to the following format, including decimal separators and suggested alignment:
Volume LABEL
Disk ID is XX

nnn,nnn bytes total disk space
 nn,nnn bytes allocated in x files
  n,nnn bytes available on disk

    256 bytes in each block (254 usable)
    nnn total blocks on disk
    nnn available blocks on disk

 nn,nnn total bytes memory
 nn,nnn bytes free