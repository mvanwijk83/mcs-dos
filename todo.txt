TODO

  Merge SPLASH into VER: put it under a /SPLASH switch
  Insert new line functionality in EDIT
  Manual viewer (separate program on disk)
  Proper handling of RUNning programs without a BASIC stub? (could always refuse to run them)
  Fastloader?
  FIND/FINDSTR? (useful for piping)
  Extras
   - extra charsets
   - AUTOEXEC.BAT replacements
  Spin off EDIT into a separate program? Duplicate shared code. Load into available memory space (does that even work?)
  SET CURSOR=LINE|SOLID[,COLOR]
  MCSD
   - separate program
   - see output from "assess" convo
   - can we determine SID model/revision?
   - other SID info (volume?)
   - live joystick/paddle/mouse tests
   - make it look like as much as MSD as possible including colors!
  Git repo

Archive the current CC65-based baseline and store it in a .ZIP in the project folder. We will now continue on an Oscar64 

Final steps for release:
 - Check 1541-II, 1571 and 1581 compatibility & fix where possible
 - Create a suitable filesystem structure to put in Git & publish on GitHub




--------------------------------------------------------------------------------

  CHARSET command

Changes the current character set.

CHARSET [name]

  name  A character set name matching
        a CPI file without extension.

If name is omitted, the default C64 ROM
character set is enabled.